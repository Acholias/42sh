[package]
name = "fortytwo_sh"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "42sh"
path = "src/main.rs"

[dependencies]
libc = "0.2"
nix = { version = "0.29", features = ["process", "signal", "fs", "term", "ioctl"] }
